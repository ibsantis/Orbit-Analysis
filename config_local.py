"""
Copy this file to the repo root as `config_local.py` and edit paths for your machine.
This copy is ignored by git (see .gitignore).
"""
from pathlib import Path
DATA_DIR    = Path("/absolute/path/to/data")
OUTPUT_DIR  = Path("/absolute/path/to/output")
SCRATCH_DIR = Path("/absolute/path/to/scratch")
